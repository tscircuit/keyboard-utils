{
  "name": "@tscircuit/keyboard-utils",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsup-node ./lib/index.ts --format esm --dts --sourcemap"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsup": "^8.4.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
